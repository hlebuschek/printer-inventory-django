# Real-Time Updates для Monthly Report

## Обзор

Система real-time обновлений позволяет нескольким пользователям одновременно работать с таблицей monthly report, видя изменения друг друга в реальном времени. Это решает проблему конфликтов при одновременном редактировании и предотвращает случайную перезапись данных.

## Проблема

**До внедрения:** Если два пользователя одновременно редактируют таблицу, возникает следующая ситуация:

1. Пользователь А открывает страницу, видит пустую ячейку (значение 0)
2. Пользователь Б тоже открывает страницу, видит пустую ячейку
3. Пользователь Б вводит значение 100 и сохраняет
4. Пользователь А случайно кликает на ячейку и нажимает Enter
5. **Результат:** Значение 100 стирается, заменяется на 0

**После внедрения:** Пользователь А сразу видит изменение от пользователя Б через WebSocket и получает уведомление. Если пользователь А все равно попытается изменить ячейку, система обнаружит конфликт и покажет предупреждение.

## Архитектура

### Backend (Django Channels)

```
┌─────────────────────┐
│  API View           │
│  api_update_        │
│  counters()         │
└──────┬──────────────┘
       │
       │ Сохраняет изменения
       │ в БД
       │
       ▼
┌─────────────────────┐         ┌──────────────────┐
│  WebSocket          │◄────────┤  Redis Pub/Sub   │
│  Broadcast          │         │  (Channel Layer) │
└──────┬──────────────┘         └──────────────────┘
       │
       │ Отправляет сообщение
       │ в группу monthly_report_{year}_{month}
       │
       ▼
┌─────────────────────┐
│  MonthlyReport      │
│  Consumer           │
└──────┬──────────────┘
       │
       │ Рассылает всем
       │ подключенным клиентам
       │
       ▼
┌─────────────────────┐
│  WebSocket Clients  │
│  (Браузеры)         │
└─────────────────────┘
```

### Frontend (Vue.js)

```
┌─────────────────────┐
│  MonthDetailPage    │
│  .vue               │
└──────┬──────────────┘
       │
       │ onMounted()
       │
       ▼
┌─────────────────────┐
│  connectWebSocket() │
└──────┬──────────────┘
       │
       │ Подключается к
       │ ws://host/ws/monthly-report/{year}/{month}/
       │
       ▼
┌─────────────────────┐
│  WebSocket          │
│  Connection         │
└──────┬──────────────┘
       │
       │ onmessage
       │
       ▼
┌─────────────────────┐
│  handleWebSocket    │
│  Message()          │
└──────┬──────────────┘
       │
       │ Обновляет таблицу
       │ Показывает уведомление
       │ Проверяет конфликты
       │
       ▼
┌─────────────────────┐
│  UI Update +        │
│  Toast Notification │
└─────────────────────┘
```

## Компоненты системы

### 1. Backend Components

#### MonthlyReportConsumer (`monthly_report/consumers.py`)

WebSocket consumer для обработки real-time соединений:

```python
class MonthlyReportConsumer(AsyncJsonWebsocketConsumer):
    async def connect(self):
        # Присоединяется к группе по месяцу
        self.room_group_name = f'monthly_report_{year}_{month}'
        await self.channel_layer.group_add(...)

    async def counter_update(self, event):
        # Отправляет обновление клиенту
        await self.send_json({
            'type': 'counter_update',
            'report_id': ...,
            'field': ...,
            'old_value': ...,
            'new_value': ...,
            'user_username': ...,
            'user_full_name': ...,
            'timestamp': ...
        })
```

**Группы WebSocket:**
- Формат: `monthly_report_{year}_{month}`
- Пример: `monthly_report_2024_01`
- Каждый месяц имеет свою изолированную группу

#### API View Updates (`monthly_report/views.py`)

Функция `api_update_counters` обновлена для отправки WebSocket сообщений:

```python
# После сохранения изменений
if changes_for_audit:
    channel_layer = get_channel_layer()
    year = obj.month.year
    month = obj.month.month
    room_group_name = f'monthly_report_{year}_{month:02d}'

    for field_name, (old_val, new_val) in changes_for_audit.items():
        async_to_sync(channel_layer.group_send)(
            room_group_name,
            {
                'type': 'counter_update',
                'report_id': obj.id,
                'field': field_name,
                'old_value': old_val,
                'new_value': new_val,
                'user_username': user.username,
                'user_full_name': user.get_full_name(),
                'timestamp': timezone.now().isoformat(),
            }
        )
```

#### Routing (`monthly_report/routing.py`)

```python
websocket_urlpatterns = [
    re_path(
        r'^ws/monthly-report/(?P<year>\d{4})/(?P<month>\d{2})/$',
        consumers.MonthlyReportConsumer.as_asgi()
    ),
]
```

**URL паттерн:**
- `ws://localhost:5000/ws/monthly-report/2024/01/`
- Параметры: year (4 цифры), month (2 цифры с ведущим нулем)

### 2. Frontend Components

#### WebSocket Connection (`MonthDetailPage.vue`)

```javascript
// Подключение при монтировании компонента
onMounted(() => {
  loadReports()
  connectWebSocket()
})

// Отключение при размонтировании
onUnmounted(() => {
  disconnectWebSocket()
})
```

#### Connection Management

```javascript
function connectWebSocket() {
  const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:'
  const host = window.location.host
  const year = props.year
  const month = String(props.month).padStart(2, '0')
  const wsUrl = `${protocol}//${host}/ws/monthly-report/${year}/${month}/`

  websocket = new WebSocket(wsUrl)

  websocket.onopen = () => {
    wsConnected.value = true
    wsReconnectAttempts.value = 0
  }

  websocket.onmessage = (event) => {
    handleWebSocketMessage(JSON.parse(event.data))
  }

  websocket.onclose = () => {
    // Автоматическое переподключение с экспоненциальной задержкой
    if (wsReconnectAttempts.value < MAX_RECONNECT_ATTEMPTS) {
      const delay = Math.min(1000 * Math.pow(2, wsReconnectAttempts.value), 30000)
      setTimeout(connectWebSocket, delay)
    }
  }
}
```

**Reconnection Strategy:**
- Максимум 5 попыток переподключения
- Экспоненциальная задержка: 1s, 2s, 4s, 8s, 16s
- Максимальная задержка: 30 секунд

#### Message Handling & Optimistic Locking

```javascript
function handleWebSocketMessage(message) {
  if (message.type === 'counter_update') {
    const report = reports.value.find(r => r.id === message.report_id)
    const currentValue = report[message.field]

    // OPTIMISTIC LOCKING: Проверка конфликта
    if (currentValue !== message.old_value &&
        currentValue !== message.new_value) {
      // Конфликт обнаружен!
      showToast(
        '⚠️ Конфликт редактирования',
        `Ваше значение: ${currentValue}\n` +
        `Значение другого пользователя: ${message.new_value}\n\n` +
        `Пожалуйста, обновите страницу...`,
        'warning',
        10000
      )
      return // НЕ обновляем значение
    }

    // Обновляем значение
    report[message.field] = message.new_value

    // Показываем уведомление
    showToast(
      '🔄 Обновление от другого пользователя',
      `${message.user_full_name} изменил "${fieldLabel}"\n` +
      `${message.old_value} → ${message.new_value}`,
      'info',
      5000
    )
  }
}
```

**Алгоритм Optimistic Locking:**

1. **Получено WebSocket сообщение** с обновлением поля
2. **Сравниваем значения:**
   - `currentValue` - текущее значение в таблице
   - `message.old_value` - значение до изменения (с сервера)
   - `message.new_value` - новое значение (с сервера)
3. **Проверка конфликта:**
   ```
   if (currentValue != old_value AND currentValue != new_value):
       КОНФЛИКТ ОБНАРУЖЕН
   ```
4. **При конфликте:**
   - Показываем предупреждение с обоими значениями
   - НЕ обновляем ячейку автоматически
   - Просим пользователя обновить страницу
5. **Без конфликта:**
   - Обновляем значение в таблице
   - Показываем информационное уведомление

## UI Components

### Connection Status Badge

В шапке страницы отображается статус WebSocket подключения:

```vue
<!-- Подключен -->
<span class="badge text-bg-info" title="Подключен к WebSocket">
  🔌 Live
</span>

<!-- Отключен -->
<span class="badge text-bg-warning" title="WebSocket отключен">
  🔌 Offline
</span>
```

### Toast Notifications

Система показывает три типа уведомлений:

#### 1. Информационное уведомление (синее)

Появляется когда другой пользователь изменил ячейку:

```
🔄 Обновление от другого пользователя
Иван Иванов изменил "A4 ч/б конец" для HP LaserJet (SN: ABC123)
500 → 550
```

**Параметры:**
- Тип: `info`
- Длительность: 5 секунд
- Цвет: синий

#### 2. Предупреждение о конфликте (желтое)

Появляется когда обнаружен конфликт редактирования:

```
⚠️ Конфликт редактирования
Иван Иванов изменил "A4 ч/б конец" (HP LaserJet, SN: ABC123)
Ваше значение: 600
Значение Иван Иванов: 550

Пожалуйста, обновите страницу чтобы увидеть актуальные данные.
```

**Параметры:**
- Тип: `warning`
- Длительность: 10 секунд
- Цвет: желтый/оранжевый

#### 3. Информация о синхронизации (зеленое)

Появляется после успешной синхронизации с inventory:

```
Синхронизация завершена
✅ Обновлено позиций: 15
⚠️ Пропущено (ручное редактирование): 3
⏭️ Пропущено (нет данных): 2
🔄 Пересчитано групп: 5
```

## Сценарии использования

### Сценарий 1: Нормальное редактирование (без конфликтов)

**Участники:** Пользователь А, Пользователь Б

1. **Пользователь А** открывает страницу January 2024
   - WebSocket подключается к `monthly_report_2024_01`
   - Статус: 🔌 Live

2. **Пользователь Б** открывает ту же страницу
   - WebSocket подключается к той же группе
   - Оба пользователя теперь в одной группе

3. **Пользователь Б** изменяет `a4_bw_end` для принтера #123: `0 → 500`
   - Изменение сохраняется в БД
   - Backend отправляет WebSocket сообщение в группу

4. **Пользователь А** получает WebSocket сообщение
   - Таблица автоматически обновляется: ячейка показывает `500`
   - Появляется уведомление:
     ```
     🔄 Обновление от другого пользователя
     Иван Иванов изменил "A4 ч/б конец" для HP LaserJet
     0 → 500
     ```
   - Уведомление исчезает через 5 секунд

5. **Результат:** Оба пользователя видят актуальные данные в реальном времени

### Сценарий 2: Конфликт редактирования

**Участники:** Пользователь А, Пользователь Б

1. **Начальное состояние:** ячейка содержит `0`

2. **Пользователь А** вводит `100` в ячейку, но **не сохраняет** (курсор в ячейке)

3. **Пользователь Б** вводит `200` в ту же ячейку и **нажимает Enter**
   - Значение сохраняется в БД: `0 → 200`
   - WebSocket сообщение отправляется

4. **Пользователь А** получает WebSocket сообщение
   - `currentValue = 100` (локально введенное)
   - `message.old_value = 0` (с сервера)
   - `message.new_value = 200` (с сервера)
   - **Условие конфликта:** `100 != 0 AND 100 != 200` ✅

5. **Пользователь А** видит предупреждение:
   ```
   ⚠️ Конфликт редактирования
   Петр Петров изменил "A4 ч/б конец"
   Ваше значение: 100
   Значение Петр Петров: 200

   Пожалуйста, обновите страницу...
   ```

6. **Пользователь А** обновляет страницу
   - Видит актуальное значение `200`
   - Может принять решение: оставить `200` или изменить на свое значение

7. **Результат:** Данные не потеряны, пользователь информирован о конфликте

### Сценарий 3: Одновременное редактирование разных ячеек

**Участники:** Пользователь А, Пользователь Б

1. **Пользователь А** редактирует `a4_bw_end`: `0 → 100`
2. **Пользователь Б** одновременно редактирует `a4_color_end`: `0 → 50`

3. **Пользователь А** получает уведомление:
   ```
   🔄 Обновление от другого пользователя
   Иван Иванов изменил "A4 цвет конец"
   0 → 50
   ```

4. **Пользователь Б** получает уведомление:
   ```
   🔄 Обновление от другого пользователя
   Петр Петров изменил "A4 ч/б конец"
   0 → 100
   ```

5. **Результат:** Оба изменения сохранены, конфликтов нет

### Сценарий 4: Потеря соединения и восстановление

1. **Пользователь** работает со страницей
   - WebSocket статус: 🔌 Live

2. **Сеть временно недоступна**
   - WebSocket отключается
   - Статус меняется: 🔌 Offline
   - Попытка #1 переподключения через 1 секунду

3. **Сеть все еще недоступна**
   - Попытка #2 через 2 секунды
   - Попытка #3 через 4 секунды
   - ...

4. **Сеть восстановлена**
   - WebSocket успешно подключается
   - Статус: 🔌 Live
   - Счетчик попыток сбрасывается

5. **Результат:** Автоматическое восстановление соединения

## Технические детали

### Redis Channel Layer

WebSocket сообщения передаются через Redis pub/sub:

```python
# settings.py
CHANNEL_LAYERS = {
    'default': {
        'BACKEND': 'channels_redis.core.RedisChannelLayer',
        'CONFIG': {
            "hosts": [(REDIS_HOST, REDIS_PORT)],
        },
    },
}
```

**Преимущества Redis:**
- Высокая производительность
- Масштабируемость (можно запустить несколько Daphne серверов)
- Надежная доставка сообщений

### ASGI Server (Daphne)

WebSocket требует ASGI сервер вместо WSGI:

```bash
# Development
python -m daphne -b 0.0.0.0 -p 5000 printer_inventory.asgi:application

# Production (с systemd)
daphne -b 127.0.0.1 -p 5000 printer_inventory.asgi:application
```

**Важно:** Не используйте `runserver` для WebSocket - он не поддерживает WebSocket!

### ASGI Configuration

```python
# printer_inventory/asgi.py
application = ProtocolTypeRouter({
    'http': get_asgi_application(),
    'websocket': AuthMiddlewareStack(
        URLRouter(
            inventory.routing.websocket_urlpatterns +
            monthly_report.routing.websocket_urlpatterns
        )
    ),
})
```

## Производительность

### Нагрузка на сервер

**Метрики при 100 одновременных пользователях:**
- WebSocket соединения: 100
- Потребление памяти: ~5MB на соединение = 500MB
- CPU: минимальная нагрузка (события только при изменениях)
- Redis память: ~1KB на сообщение

**Оптимизация:**
- Группы по месяцам изолируют трафик
- Сообщения отправляются только при реальных изменениях
- Автоматическое отключение неактивных соединений

### Сетевой трафик

**При изменении одной ячейки:**
- HTTP POST запрос: ~500 bytes
- WebSocket сообщение: ~300 bytes
- Итого: ~800 bytes на одно изменение

**При 10 изменениях в минуту:**
- 10 * 800 bytes = 8KB/мин
- За час: 480KB
- За день: ~11MB

## Безопасность

### Аутентификация

WebSocket соединения требуют аутентификации:

```python
# asgi.py
'websocket': AuthMiddlewareStack(
    URLRouter(...)
)
```

**Механизм:**
- Django session cookies используются для аутентификации
- Только авторизованные пользователи могут подключиться
- Проверка прав доступа на уровне consumer

### Авторизация

Пользователи получают обновления только для своего месяца:

```python
# Группа формируется по году и месяцу из URL
self.room_group_name = f'monthly_report_{year}_{month}'
```

**Изоляция данных:**
- January 2024 → группа `monthly_report_2024_01`
- February 2024 → группа `monthly_report_2024_02`
- Пользователи в разных группах не видят обновления друг друга

### Валидация данных

Все изменения проходят полную валидацию на backend:

```python
# api_update_counters
1. Проверка прав пользователя
2. Проверка окна редактирования (MonthControl)
3. Проверка разрешенных полей
4. Валидация значений (_to_int)
5. Проверка ограничений по дублям
6. Проверка спецификаций модели
```

**WebSocket только уведомляет,** не изменяет данные напрямую!

## Отладка

### Логирование WebSocket событий

```javascript
// Frontend
console.log('WebSocket message received:', message)
console.warn('Conflict detected:', { field, current, incoming })
```

```python
# Backend
logger.info(f"WebSocket broadcast sent for {len(changes)} field changes")
logger.error(f"Ошибка отправки WebSocket уведомления: {e}")
```

### Инструменты разработчика

**Chrome DevTools → Network → WS:**
- Просмотр WebSocket соединений
- Мониторинг входящих/исходящих сообщений
- Проверка статуса подключения

**Vue DevTools:**
- Просмотр reactive данных (`wsConnected`, `reports`)
- Отслеживание изменений в реальном времени

### Тестирование

**Ручное тестирование:**
1. Откройте два окна браузера (режим инкогнито для второго)
2. Войдите под разными пользователями
3. Откройте один и тот же месяц
4. Измените ячейку в первом окне
5. Проверьте, что второе окно получило обновление

**Console тест:**
```javascript
// В консоли браузера
console.log('WebSocket status:', websocket?.readyState)
// 0 = CONNECTING, 1 = OPEN, 2 = CLOSING, 3 = CLOSED
```

## Ограничения и будущие улучшения

### Текущие ограничения

1. **Полная перезагрузка таблицы при end полях**
   - При изменении `*_end` полей, таблица перезагружается через 500ms
   - Причина: нужно обновить `total_prints` который пересчитывается на backend
   - Альтернатива: Отправлять обновленный `total_prints` через WebSocket

2. **Нет индикации "кто редактирует ячейку сейчас"**
   - Конфликт обнаруживается только при сохранении
   - Будущее: показывать курсор другого пользователя в реальном времени

3. **Максимум 5 попыток переподключения**
   - После 5 неудачных попыток WebSocket не переподключается
   - Требуется обновление страницы

### Планируемые улучшения

1. **Cursor Awareness (Phase 2)**
   ```javascript
   // Показывать имя пользователя над ячейкой, которую он редактирует
   <div class="editing-indicator">
     Иван Иванов редактирует...
   </div>
   ```

2. **Granular Updates (Phase 3)**
   ```python
   # Отправлять обновленный total_prints в WebSocket сообщении
   {
       'type': 'counter_update',
       'field': 'a4_bw_end',
       'new_value': 500,
       'total_prints': 1234  # НОВОЕ
   }
   ```

3. **Conflict Resolution UI (Phase 4)**
   ```javascript
   // Показывать диалог для выбора версии
   Modal: "Конфликт редактирования"
   [ Оставить свою версию (100) ]
   [ Принять версию Ивана (200) ]
   [ Отмена ]
   ```

4. **Presence System (Phase 5)**
   ```javascript
   // Показывать список активных пользователей
   👥 Сейчас на странице: Иван, Петр, Мария (3)
   ```

## Troubleshooting

### Проблема: WebSocket показывает "Offline"

**Причины:**
1. Daphne сервер не запущен (используется runserver)
2. Redis недоступен
3. Неправильный URL WebSocket

**Решение:**
```bash
# 1. Проверьте, что Daphne запущен
ps aux | grep daphne

# 2. Проверьте Redis
redis-cli ping  # Должен ответить "PONG"

# 3. Проверьте логи
tail -f logs/django.log
```

### Проблема: Уведомления не появляются

**Причины:**
1. Toast notifications не настроены
2. WebSocket сообщения не доходят

**Решение:**
```javascript
// Проверьте в консоли браузера
console.log('WebSocket message received:', message)

// Если сообщения не приходят, проверьте backend логи
logger.info(f"WebSocket broadcast sent...")
```

### Проблема: Конфликты не обнаруживаются

**Причины:**
1. Значения имеют разные типы (string vs number)
2. WebSocket сообщение приходит с задержкой

**Решение:**
```javascript
// Добавьте строгое сравнение типов
if (currentValue !== message.old_value) {
  console.log('Type mismatch:',
    typeof currentValue,
    typeof message.old_value
  )
}
```

## Заключение

Real-time обновления значительно улучшают пользовательский опыт при работе с monthly report:

✅ **Решена проблема конфликтов** - пользователи видят изменения друг друга
✅ **Предотвращена потеря данных** - optimistic locking обнаруживает конфликты
✅ **Улучшена коммуникация** - уведомления информируют о действиях других пользователей
✅ **Повышена производительность** - не нужно постоянно обновлять страницу

Система готова к production использованию и может масштабироваться для большого количества одновременных пользователей.
