# Умный импорт принтеров с автоопросом через SNMP

## Что это?

**Новая команда `import_printers_with_snmp`** - это умный импорт, который:

1. ✅ **Опрашивает через SNMP** каждый принтер сразу при импорте
2. ✅ **Извлекает данные**: модель, производитель, серийник, MAC
3. ✅ **Fuzzy matching моделей** - находит похожие модели в справочнике (HP ≈ Hewlett-Packard)
4. ✅ **Проверяет дубликаты** серийников в ContractDevice
5. ✅ **Обнаруживает прошитые модели** (MAC совпадает, серийник другой)
6. ✅ **Создает принтер** с уже проставленной моделью из справочника

## Преимущества перед обычным импортом

| Обычный импорт | Умный импорт с SNMP |
|----------------|---------------------|
| 1. Импорт IP → принтеры без данных | 1. Импорт IP → **автоопрос** → принтеры с данными |
| 2. Ручной опрос через UI | ~~2. Ручной опрос~~ **Уже сделано!** |
| 3. Команда `migrate_models_to_devicemodel` | ~~3. Миграция моделей~~ **Уже сделано!** |
| **Итого:** 3 шага, долго | **Итого:** 1 шаг, быстро |

## Быстрый старт

### Шаг 1: Подготовь Excel

Минимальный вариант:
```csv
IP адрес
192.168.1.100
192.168.1.101
192.168.1.102
```

С организацией:
```csv
IP адрес,Организация
192.168.1.100,Новая орг
192.168.1.101,Новая орг
```

С MAC (для прошитых моделей):
```csv
IP адрес,MAC адрес,Организация
192.168.1.100,AA:BB:CC:DD:EE:FF,Новая орг
192.168.1.101,,Новая орг
```

### Шаг 2: Запусти команду

**Dry-run (проверка без сохранения):**
```bash
python manage.py import_printers_with_snmp printers.xlsx --dry-run --show-details
```

**Реальный импорт:**
```bash
python manage.py import_printers_with_snmp printers.xlsx --auto-create-org
```

### Шаг 3: Всё готово!

Принтеры добавлены с:
- ✅ Серийными номерами
- ✅ MAC адресами
- ✅ Моделями из справочника (где найдено совпадение)
- ✅ Счетчиками

## Подробные примеры

### Пример 1: Импорт с детальным выводом

```bash
python manage.py import_printers_with_snmp printers.xlsx \
  --dry-run \
  --show-details \
  --auto-create-org
```

**Вывод:**
```
=== Умный импорт принтеров с SNMP опросом ===
Файл: printers.xlsx
Fuzzy matching threshold: 0.75

Найдены столбцы:
  IP адрес: колонка 1
  Организация: колонка 2

📡 Опрос 192.168.1.100 (строка 2)...
  SNMP данные:
    Модель: HP LaserJet M1522nf MFP
    Производитель: Hewlett-Packard
    Серийник: XXXXXXXXXX
    MAC: 00:23:7D:75:A5:A8
    ✅ Модель найдена: HP LaserJet M1522nf

  ✅ OK: 192.168.1.100 [Новая орг] | SN:XXXXXXXXXX | Модель:HP LaserJet M1522nf

📡 Опрос 192.168.1.101 (строка 3)...
  SNMP данные:
    Модель: Ricoh MP C2004
    Производитель: Ricoh
    Серийник: Y1234567890
    MAC: 11:22:33:44:55:66
    ✅ Модель найдена: Ricoh MP C2004

  ✅ OK: 192.168.1.101 [Новая орг] | SN:Y1234567890 | Модель:Ricoh MP C2004

======================================================================
📊 ИТОГОВАЯ СТАТИСТИКА:
  Всего строк обработано: 2
  ✅ Успешно подготовлено: 2
  📡 SNMP опросов выполнено: 2
  🎯 Моделей найдено в справочнике: 2

✅ Для применения изменений запустите без --dry-run
```

### Пример 2: Модель не найдена в справочнике

```bash
python manage.py import_printers_with_snmp printers.xlsx --dry-run --show-details
```

**Вывод:**
```
📡 Опрос 192.168.1.102 (строка 4)...
  SNMP данные:
    Модель: HP LaserJet Pro M404dn
    Производитель: HP
    Серийник: ABC123
    MAC: AA:BB:CC:DD:EE:FF
    ⚠️  Модель не найдена в справочнике
    Похожие модели:
      - HP LaserJet Pro M404dw (score=0.92)
      - HP LaserJet Pro M402dn (score=0.87)
      - HP LaserJet Pro M405dn (score=0.85)

  ✅ OK: 192.168.1.102 [Новая орг] | SN:ABC123 | Модель:HP LaserJet Pro M404dn

======================================================================
⚠️  МОДЕЛИ НЕ НАЙДЕНЫ В СПРАВОЧНИКЕ:
  Строка 4: 192.168.1.102 - модель 'HP LaserJet Pro M404dn'

💡 РЕКОМЕНДАЦИИ:
  Добавьте отсутствующие модели в справочник contracts.DeviceModel
  Или попробуйте снизить порог fuzzy matching: --fuzzy-threshold 0.65
```

**Решение:** Добавь модель в справочник:
```bash
python manage.py shell
```
```python
from contracts.models import Manufacturer, DeviceModel

hp = Manufacturer.objects.get_or_create(name="HP")[0]
DeviceModel.objects.create(
    manufacturer=hp,
    name="LaserJet Pro M404dn",
    device_type="printer",
    has_network_port=True
)
```

Затем повтори импорт:
```bash
python manage.py import_printers_with_snmp printers.xlsx
```

### Пример 3: Fuzzy matching с низким порогом

Если модель не находится при пороге 0.75, попробуй снизить:

```bash
python manage.py import_printers_with_snmp printers.xlsx \
  --fuzzy-threshold 0.65 \
  --dry-run \
  --show-details
```

Это найдет более "мягкие" совпадения:
- `HP LaserJet Pro M404dn` ≈ `HP LaserJet Pro M402dn` (score=0.87)
- `Ricoh MP C2004 MFP` ≈ `Ricoh MP C2004` (score=0.95)

### Пример 4: Обнаружение прошитых моделей

```csv
IP адрес,MAC адрес,Организация
192.168.1.100,AA:BB:CC:DD:EE:FF,Альфа
192.168.1.200,AA:BB:CC:DD:EE:FF,Бета
```

**Вывод:**
```
📡 Опрос 192.168.1.100 (строка 2)...
  SNMP данные:
    Серийник: SN001
    MAC: AA:BB:CC:DD:EE:FF
  ✅ OK: 192.168.1.100 [Альфа] | SN:SN001

📡 Опрос 192.168.1.200 (строка 3)...
  SNMP данные:
    Серийник: SN002
    MAC: AA:BB:CC:DD:EE:FF
    🔧 ПРОШИТАЯ МОДЕЛЬ: MAC AA:BB:CC:DD:EE:FF используется принтером 192.168.1.100
       (SN: SN001), но серийник отличается (SN002)

💡 ПРОШИТЫЕ МОДЕЛИ:
  Обнаружены принтеры с одинаковым MAC, но разными серийниками
  Это нормально для прошитых моделей - MAC используется как уникальный ID
```

### Пример 5: Пропуск ошибок SNMP

Если некоторые принтеры недоступны:

```bash
python manage.py import_printers_with_snmp printers.xlsx \
  --skip-poll-failures \
  --auto-create-org
```

**Вывод:**
```
📡 Опрос 192.168.1.100 (строка 2)...
  ❌ POLL_FAILED: Connection timeout

📡 Опрос 192.168.1.101 (строка 3)...
  ✅ OK: 192.168.1.101 [Новая орг] | SN:ABC123

======================================================================
📊 ИТОГОВАЯ СТАТИСТИКА:
  Всего строк обработано: 2
  ✅ Успешно подготовлено: 1
  ❌ SNMP опросов не удалось: 1

❌ ОШИБКИ SNMP ОПРОСА:
  Строка 2: 192.168.1.100 - Connection timeout
```

## Параметры команды

| Параметр | Описание | Пример |
|----------|----------|--------|
| `xlsx_file` | Путь к Excel файлу | `printers.xlsx` |
| `--dry-run` | Показать результаты без сохранения | |
| `--auto-create-org` | Автоматически создавать организации | |
| `--show-details` | Детальный вывод по каждому принтеру | |
| `--fuzzy-threshold` | Порог fuzzy matching (0.0-1.0, по умолчанию 0.75) | `--fuzzy-threshold 0.65` |
| `--skip-poll-failures` | Пропускать принтеры с ошибками SNMP | |

## Fuzzy Matching - как это работает?

### Принцип работы

Команда нормализует названия моделей для сравнения:

**Исходное название:**
```
HP LaserJet Pro M404dn MFP
```

**После нормализации:**
```
laserjet pro m404
```

**Что убирается:**
- Производитель (HP, Hewlett-Packard)
- Суффиксы (MFP, ADF, duplex, dn, nf, dw...)
- Спецсимволы
- Лишние пробелы

**Затем сравнивается с моделями в справочнике:**
```
HP LaserJet Pro M404dn (score=1.00) ← точное совпадение
HP LaserJet Pro M404dw (score=0.92) ← похожая
HP LaserJet Pro M402dn (score=0.87) ← похожая
```

### Поддерживаемые алиасы производителей

| Канонический | Алиасы |
|--------------|--------|
| hp | HP, Hewlett-Packard, Hewlett Packard, HP Inc, HP Inc. |
| ricoh | Ricoh |
| kyocera | Kyocera, Kyocera Mita |
| canon | Canon |
| epson | Epson, Seiko Epson |
| brother | Brother |
| konica | Konica, Konica Minolta, Konica-Minolta |

### Игнорируемые суффиксы

- **Функциональные:** MFP, MF, Multifunction, ADF, Duplex
- **Сетевые:** Network, Wireless, WiFi, USB, Ethernet
- **Цвет:** Color, Mono
- **HP суффиксы:** n, dn, nf, dnf, fn, df, dw, ne
- **Маркетинг:** Series, Plus, Pro, Enterprise, Office

## FAQ

### Q: Зачем нужна эта команда, если есть обычный импорт?

**A:** Умный импорт экономит время и автоматизирует рутину:

**Без умного импорта:**
```bash
# 1. Импорт
python manage.py import_printers_xlsx printers.xlsx
# 2. Ручной опрос 100 принтеров через web UI (долго!)
# 3. Миграция моделей
python manage.py migrate_models_to_devicemodel
```
⏱️ Время: 2-3 часа

**С умным импортом:**
```bash
python manage.py import_printers_with_snmp printers.xlsx --auto-create-org
```
⏱️ Время: 10-15 минут

### Q: Что делать если SNMP опрос не работает для некоторых принтеров?

**A:** Используй `--skip-poll-failures`:

```bash
python manage.py import_printers_with_snmp printers.xlsx --skip-poll-failures
```

Принтеры с ошибками будут пропущены, остальные импортируются.

### Q: Как добавить модель в справочник?

**A:** Через админку или shell:

```python
from contracts.models import Manufacturer, DeviceModel

# Найти или создать производителя
hp = Manufacturer.objects.get_or_create(name="HP")[0]

# Создать модель
DeviceModel.objects.create(
    manufacturer=hp,
    name="LaserJet Pro M404dn",  # ← точно как в SNMP!
    device_type="printer",
    has_network_port=True
)
```

### Q: Почему fuzzy matching не находит модель?

**A:** Две причины:

1. **Модели нет в справочнике** → добавь модель
2. **Порог слишком высокий** → снизь: `--fuzzy-threshold 0.65`

Команда покажет похожие модели с их score - это поможет понять причину.

### Q: Можно ли использовать обе команды?

**A:** Да!

- **`import_printers_with_snmp`** - для НОВЫХ принтеров (с автоопросом)
- **`import_printers_xlsx`** - для ОБНОВЛЕНИЯ существующих (без опроса)

### Q: Как обрабатываются прошитые модели?

**A:** Команда обнаруживает их автоматически:

- Если MAC совпадает, но серийник другой → **прошитая модель** (warning, но импорт продолжается)
- Если MAC и серийник совпадают полностью → **коллизия** (ошибка, пропуск)

## Примеры реальных сценариев

### Сценарий 1: Импорт новой организации (50 принтеров)

```bash
# 1. Подготовь Excel
echo "IP адрес,Организация" > printers.csv
echo "192.168.1.100,Новая орг" >> printers.csv
# ... добавь остальные IP

# 2. Dry-run для проверки
python manage.py import_printers_with_snmp printers.csv --dry-run --show-details --auto-create-org

# 3. Реальный импорт
python manage.py import_printers_with_snmp printers.csv --auto-create-org

# 4. Проверка
python manage.py shell
>>> from inventory.models import Organization
>>> org = Organization.objects.get(name="Новая орг")
>>> org.printers.count()
50
>>> org.printers.filter(device_model__isnull=False).count()
48  # 48 моделей найдено, 2 нужно добавить вручную

# 5. Через 3 месяца - добавление в contracts
python manage.py contracts_import_xlsx договор.xlsx
python manage.py link_devices_by_serial
```

### Сценарий 2: Импорт с прошитыми моделями

```bash
# Excel с указанными MAC адресами для прошитых
# IP адрес,MAC адрес,Организация
# 192.168.1.100,AA:BB:CC:DD:EE:FF,Альфа
# 192.168.1.200,AA:BB:CC:DD:EE:FF,Бета  ← прошитая

python manage.py import_printers_with_snmp printers.xlsx --auto-create-org --show-details

# Вывод покажет:
# 🔧 ПРОШИТАЯ МОДЕЛЬ: MAC AA:BB:CC:DD:EE:FF используется принтером 192.168.1.100
#    (SN: SN001), но серийник отличается (SN002)

# Оба принтера будут импортированы, MAC используется как уникальный ID
```

### Сценарий 3: Поиск моделей с низким порогом

```bash
# Первая попытка (стандартный порог)
python manage.py import_printers_with_snmp printers.xlsx --dry-run --show-details

# Вывод:
# ⚠️  Моделей НЕ найдено: 5

# Вторая попытка (низкий порог)
python manage.py import_printers_with_snmp printers.xlsx --fuzzy-threshold 0.60 --dry-run

# Вывод:
# 🎯 Моделей найдено в справочнике: 48
# ⚠️  Моделей НЕ найдено: 2  ← улучшение!

# Реальный импорт
python manage.py import_printers_with_snmp printers.xlsx --fuzzy-threshold 0.60 --auto-create-org
```

## Сравнение команд

| Функция | `import_printers_xlsx` | `import_printers_with_snmp` |
|---------|------------------------|------------------------------|
| Скорость импорта | ⚡ Быстро | 🐌 Медленно (SNMP опросы) |
| SNMP опрос при импорте | ❌ Нет | ✅ Да |
| Fuzzy matching моделей | ❌ Нет | ✅ Да |
| Автоматическое заполнение серийников | ❌ Нет | ✅ Да |
| Автоматическое заполнение MAC | ❌ Нет | ✅ Да |
| Автоматическое заполнение моделей | ❌ Нет | ✅ Да (с fuzzy matching) |
| Обнаружение прошитых моделей | ❌ Нет | ✅ Да |
| Проверка дубликатов в contracts | ❌ Нет | ✅ Да |
| Использование | Обновление существующих | Импорт новых принтеров |

## Резюме

**`import_printers_with_snmp`** - это умный импорт "всё в одном":

✅ **Один шаг вместо трех:**
```bash
# Было:
import → опрос → migrate_models

# Стало:
import_printers_with_snmp
```

✅ **Автоматическое заполнение данных:**
- Серийники
- MAC адреса
- Модели (с fuzzy matching)
- Счетчики

✅ **Умная обработка:**
- Обнаружение прошитых моделей
- Проверка дубликатов
- Нечеткий поиск моделей

✅ **Готово к использованию:**
Принтеры сразу готовы к работе, не нужно ничего дополнительно настраивать!

---

**Следующий шаг:** См. `docs/QUICK_START_NEW_ORG.md` для полного workflow новой организации
