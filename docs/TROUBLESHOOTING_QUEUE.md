# Решение проблемы с накоплением очереди Celery

**Дата:** 2026-01-08
**Проблема:** Очередь `low_priority` накапливает задачи (17,972), но не обрабатывается
**Основная причина:** Celery Workers не запущены или зависли

---

## 🔍 Диагностика

Запустите диагностический скрипт на продакшн-сервере:

```bash
cd /var/www/printer-inventory
bash scripts/utils/diagnose_queue_problem.sh
```

Этот скрипт покажет:
- ✅ Запущены ли Workers и Beat
- 📊 Размер очередей Redis
- 📄 Состояние логов
- ⚙️ Переменные окружения
- 💡 Рекомендации по исправлению

---

## 🔴 Симптом: Workers не запущены

### Проверка:
```bash
ps aux | grep -E 'celery.*worker' | grep -v grep
```

**Если вывод пустой** → Workers не запущены!

### Причины:
1. Сервис `celery-worker.service` не запущен
2. Сервис упал и не перезапустился
3. Ошибка в конфигурации

### Решение:

#### Вариант 1: Через systemd (рекомендуется)

```bash
# Проверяем статус
sudo systemctl status celery-worker.service

# Если неактивен - запускаем
sudo systemctl start celery-worker.service

# Проверяем логи
sudo journalctl -u celery-worker.service -n 50 --no-pager

# Добавляем в автозагрузку (если не добавлено)
sudo systemctl enable celery-worker.service
```

#### Вариант 2: Вручную (для тестирования)

```bash
cd /var/www/printer-inventory
source .venv/bin/activate

# Запускаем воркеры в фоне
./start_workers.sh &

# Проверяем что запустились
ps aux | grep celery
```

---

## 🟡 Симптом: Очередь переполнена (>20,000 задач)

### Проблема:
- Задачи создаются быстрее, чем обрабатываются
- Workers не успевают или работают слишком медленно
- Возможно были остановлены на долгое время

### Решение:

#### Шаг 1: Остановите создание новых задач

```bash
# Остановите Celery Beat (чтобы не создавались новые задачи)
sudo systemctl stop celery-beat.service

# Или убейте процесс вручную
sudo pkill -f 'celery.*beat'
```

#### Шаг 2: Очистите очередь

**ВНИМАНИЕ:** Это удалит все необработанные задачи!

```bash
cd /var/www/printer-inventory
bash scripts/celery/clear_queues.sh
```

Скрипт:
1. Покажет текущий размер очередей
2. Попросит подтверждение (введите `yes`)
3. Удалит все задачи
4. Покажет результат

**Альтернатива - ручная очистка через redis-cli:**

```bash
# Очистка конкретной очереди
redis-cli -n 3 DEL low_priority

# Или всех очередей
redis-cli -n 3 FLUSHDB
```

#### Шаг 3: Настройте защиту от переполнения

Добавьте в `/var/www/printer-inventory/.env`:

```bash
# Максимальный размер очереди (защита от переполнения)
MAX_QUEUE_SIZE=10000

# Интервал опроса (увеличьте если Workers не успевают)
POLL_INTERVAL_MINUTES=60

# Защита от Kyocera bug
ANOMALY_CHECK_TIME_WINDOW_HOURS=24
ANOMALY_JUMP_THRESHOLD=5000
ANOMALY_SKIP_CHECK_DAYS=30
```

#### Шаг 4: Запустите сервисы

```bash
# Запускаем Workers
sudo systemctl start celery-worker.service

# Ждём 10 секунд для инициализации
sleep 10

# Проверяем что запустились
ps aux | grep celery

# Запускаем Beat (создание задач)
sudo systemctl start celery-beat.service
```

#### Шаг 5: Мониторинг

```bash
# Проверяем размер очереди каждую минуту
watch -n 60 'redis-cli -n 3 LLEN low_priority'

# Логи Workers (должны показывать обработку задач)
tail -f /var/www/printer-inventory/logs/celery.log

# Логи Beat
tail -f /var/log/celery/beat.log
```

---

## ⚠️ Симптом: Несколько процессов Beat

### Проблема:
```bash
ps aux | grep -E 'celery.*beat' | grep -v grep
# Показывает 2+ процесса
```

Это создаёт дублирующие задачи!

### Причины:
- У вас 2 systemd сервиса для Beat:
  - `celery-beat.service`
  - Возможно другой с похожим именем
- Запущен вручную и через systemd одновременно

### Решение:

```bash
# 1. Останавливаем всё
sudo pkill -9 -f 'celery.*beat'

# 2. Проверяем какие сервисы есть
ls -la /etc/systemd/system/celery-beat*

# 3. Оставляем ТОЛЬКО ОДИН сервис
# Удаляем дубликаты:
sudo rm /etc/systemd/system/celery-beat-old.service  # пример

# 4. Перезагружаем systemd
sudo systemctl daemon-reload

# 5. Запускаем правильный сервис
sudo systemctl start celery-beat.service

# 6. Проверяем что запущен ТОЛЬКО 1 процесс
ps aux | grep -E 'celery.*beat' | grep -v grep | wc -l
# Должно быть: 1
```

---

## 📊 Расчёт производительности Workers

### Формула:
```
Worker Capacity = Количество воркеров × Concurrency × 360 задач/час
```

Где **360** = (60 мин × 60 сек) / 10 сек на задачу

### Ваша текущая конфигурация:

Посмотрите `start_workers.sh`:
```bash
cat /var/www/printer-inventory/start_workers.sh
```

Ожидаемая конфигурация:
- **Воркер 1:** `high_priority`, concurrency=4
- **Воркер 2:** `low_priority`, concurrency=2
- **Воркер 3:** `daemon`, concurrency=1

**Capacity:** примерно 2 × 360 = 720 задач/час для `low_priority`

### Если Workers не успевают:

#### Вариант 1: Увеличить concurrency

Отредактируйте `start_workers.sh`:

```bash
# Было:
celery -A printer_inventory worker \
    --queues=low_priority \
    --concurrency=2 \
    ...

# Станет:
celery -A printer_inventory worker \
    --queues=low_priority \
    --concurrency=4 \
    ...
```

#### Вариант 2: Запустить больше воркеров

```bash
# Добавьте второй воркер для low_priority
celery -A printer_inventory worker \
    --queues=low_priority \
    --loglevel=INFO \
    --concurrency=2 \
    --max-tasks-per-child=200 \
    --hostname=worker_low_2@%h &
```

#### Вариант 3: Уменьшить частоту опроса

В `.env`:
```bash
# Было: опрос каждый час
POLL_INTERVAL_MINUTES=60

# Станет: опрос каждые 2 часа
POLL_INTERVAL_MINUTES=120
```

---

## 🧹 Автоматическая очистка переполнения

Система имеет встроенную защиту:

### 1. Задача `cleanup_queue_if_needed`

Запускается **каждый час в XX:55** (за 5 минут до `inventory_daemon_task`)

**Что делает:**
- Проверяет размер очереди `low_priority`
- Если размер > `MAX_QUEUE_SIZE × 2` (по умолчанию 20,000)
- Удаляет старые задачи, оставляя последние `MAX_QUEUE_SIZE`

**Проверка работы:**

```bash
# Логи должны содержать (если срабатывала):
grep "QUEUE CLEANUP" /var/www/printer-inventory/logs/celery.log

# Пример лога:
# ⚠️  QUEUE CLEANUP TRIGGERED: low_priority has 21,456 tasks (limit: 20,000)
# Removing 11,456 old tasks from queue...
# ✅ Queue cleanup completed: removed 11,456 tasks, new size: 10,000
```

### 2. Задача `inventory_daemon_task` с проверкой

Запускается **каждый час в XX:00**

**Что делает:**
- Проверяет размер очереди ПЕРЕД созданием новых задач
- Если размер > `MAX_QUEUE_SIZE` → **пропускает этот запуск**

**Проверка работы:**

```bash
# Логи должны содержать (если срабатывала):
grep "QUEUE OVERFLOW PROTECTION" /var/www/printer-inventory/logs/celery.log

# Пример лога:
# ⚠️  QUEUE OVERFLOW PROTECTION: Queue size (15,234) exceeds limit (10,000).
# Skipping this run to prevent Redis overflow.
```

### Если автоматическая очистка НЕ работает:

**Причина:** Celery Beat не запущен или зависает

**Решение:** См. раздел "Симптом: Workers не запущены"

---

## 📝 Полный алгоритм исправления

### Вариант A: Быстрое исправление (если срочно)

```bash
# 1. Остановить всё
sudo systemctl stop celery-worker.service celery-beat.service
sudo pkill -9 -f celery

# 2. Очистить очередь
redis-cli -n 3 DEL low_priority

# 3. Запустить заново
sudo systemctl start celery-worker.service
sleep 5
sudo systemctl start celery-beat.service

# 4. Проверить
ps aux | grep celery
redis-cli -n 3 LLEN low_priority
```

### Вариант B: Правильное исправление (рекомендуется)

```bash
# 1. Диагностика
cd /var/www/printer-inventory
bash scripts/utils/diagnose_queue_problem.sh

# 2. Останавливаем сервисы
sudo systemctl stop celery-worker.service celery-beat.service

# 3. Убиваем зависшие процессы
sudo pkill -9 -f celery

# 4. Проверяем конфигурацию
cat .env | grep -E 'MAX_QUEUE_SIZE|POLL_INTERVAL|REDIS_HOST'

# Если нет - добавляем:
echo "MAX_QUEUE_SIZE=10000" >> .env
echo "POLL_INTERVAL_MINUTES=60" >> .env

# 5. Очищаем очередь (интерактивно с подтверждением)
bash scripts/celery/clear_queues.sh

# 6. Проверяем systemd сервисы
ls -la /etc/systemd/system/celery-*.service

# Если есть дубликаты - удаляем старые
# sudo rm /etc/systemd/system/celery-beat-old.service
sudo systemctl daemon-reload

# 7. Очищаем старые schedule файлы
sudo rm -f /var/run/celery/celerybeat-schedule*
sudo rm -f /var/www/printer-inventory/celerybeat-schedule.db*

# 8. Запускаем Workers
sudo systemctl start celery-worker.service

# 9. Проверяем что запустились
sleep 5
ps aux | grep 'celery.*worker' | grep -v grep

# 10. Запускаем Beat
sudo systemctl start celery-beat.service

# 11. Мониторим 5 минут
watch -n 10 'echo "=== Processes ==="; ps aux | grep celery | grep -v grep; echo ""; echo "=== Queue Sizes ==="; redis-cli -n 3 LLEN low_priority; redis-cli -n 3 LLEN high_priority; redis-cli -n 3 LLEN daemon'
```

---

## 🔍 Мониторинг после исправления

### В течение первого часа:

**Каждые 5 минут проверяйте:**

```bash
# Размер очереди (должна стабилизироваться)
redis-cli -n 3 LLEN low_priority

# Процессы Workers (должны быть активны)
ps aux | grep 'celery.*worker' | grep -v grep | wc -l

# Последние логи Workers (должна быть активность)
tail -n 20 /var/www/printer-inventory/logs/celery.log
```

### Признаки нормальной работы:

✅ **Размер очереди:**
- Растёт в XX:00 (когда Beat создаёт задачи)
- Постепенно уменьшается (Workers обрабатывают)
- Стабилизируется на уровне 0-500 задач

✅ **Логи Workers:**
```
[INFO] Starting inventory for printer 123 (192.168.1.100)
[INFO] Inventory completed for printer 123: SUCCESS
```

✅ **Логи Beat:**
```
[INFO] Scheduler: Sending due task inventory-daemon-every-hour
```

### Признаки проблемы:

❌ **Очередь только растёт:**
→ Workers не успевают → Увеличьте concurrency

❌ **Логи Workers пустые:**
→ Workers зависли → Перезапустите сервис

❌ **Очередь не растёт вообще:**
→ Beat не работает → Проверьте `celery-beat.service`

---

## 📚 Дополнительные ресурсы

- **Документация по управлению очередями:** `docs/QUEUE_MANAGEMENT.md`
- **Исправление Celery Beat:** `docs/CELERY_BEAT_FIX_DEPLOYMENT.md`
- **Скрипты диагностики:** `scripts/utils/check_beat_working.sh`
- **Скрипт очистки:** `scripts/celery/clear_queues.sh`

---

## ❓ FAQ

**Q: Можно ли просто удалить все задачи из Redis?**
A: Да, но тогда принтеры не будут опрошены в этот час. Лучше запустить Workers и дать им обработать задачи.

**Q: Сколько времени Workers обрабатывают 17,972 задач?**
A: С конфигурацией по умолчанию (concurrency=2): ~17,972 / 720 ≈ **25 часов**. Рекомендуется очистить очередь и начать с нуля.

**Q: Почему очередь переполнилась?**
A: Скорее всего Workers были остановлены, а Beat продолжал создавать задачи каждый час.

**Q: Как предотвратить повторение?**
A:
1. Установите `MAX_QUEUE_SIZE=10000` в `.env`
2. Убедитесь что `celery-worker.service` добавлен в автозагрузку
3. Настройте мониторинг (см. ниже)

**Q: Нужно ли увеличивать память Redis?**
A: Очередь из 20,000 задач занимает ~50-100 MB. Redis обычно справляется. Проблема в обработке, а не в памяти.

---

## 🚨 Настройка мониторинга (рекомендуется)

Создайте cron задачу для мониторинга:

```bash
# Редактируем crontab для root
sudo crontab -e

# Добавляем:
*/30 * * * * /var/www/printer-inventory/scripts/celery/check_celery_tasks.py >> /var/log/celery/queue_monitor.log 2>&1
```

Это будет проверять очередь каждые 30 минут и логировать результат.

**Настройка алертов (опционально):**

Создайте скрипт `/var/www/printer-inventory/scripts/utils/alert_queue_overflow.sh`:

```bash
#!/bin/bash
QUEUE_SIZE=$(redis-cli -n 3 LLEN low_priority)
MAX_SIZE=15000

if [ $QUEUE_SIZE -gt $MAX_SIZE ]; then
    echo "ALERT: Queue overflow! Size: $QUEUE_SIZE" | mail -s "Celery Queue Alert" admin@example.com
fi
```

Добавьте в crontab:
```
*/15 * * * * /var/www/printer-inventory/scripts/utils/alert_queue_overflow.sh
```

---

**Автор:** Claude AI
**Дата создания:** 2026-01-08
**Последнее обновление:** 2026-01-08
