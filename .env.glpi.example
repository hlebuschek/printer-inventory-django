# ========================================
# GLPI Integration Configuration
# ========================================

# GLPI REST API URL (без слеша в конце)
GLPI_API_URL=https://glpi.yourcompany.com/apirest.php

# App Token (получить в GLPI: Setup → General → API)
GLPI_APP_TOKEN=your_app_token_here

# User Token (получить в GLPI: Setup → Users → [User] → Remote access keys)
GLPI_USER_TOKEN=your_user_token_here

# Альтернативно можно использовать username/password вместо user_token:
# GLPI_USERNAME=api_user
# GLPI_PASSWORD=secret_password

# ========================================
# SSL сертификат
# ========================================

# Проверять SSL сертификат при подключении к GLPI API (по умолчанию True)
# Установите False только для тестовых серверов с самоподписанными сертификатами!
GLPI_VERIFY_SSL=True

# ========================================
# Настройки поиска принтеров
# ========================================

# ID поля для серийного номера в GLPI (по умолчанию 5)
GLPI_SERIAL_FIELD_ID=5

# ID кастомного поля "серийный номер на бирке" (если используется плагин)
# Оставьте пустым, если не используете кастомное поле
# Пример: GLPI_LABEL_SERIAL_FIELD_ID=12345
GLPI_LABEL_SERIAL_FIELD_ID=

# ========================================
# Инструкции по получению токенов
# ========================================

# 1. App Token:
#    - Войдите в GLPI как администратор
#    - Setup → General → API
#    - Включите REST API (Enable REST API)
#    - Скопируйте "Application token (app_token)" или создайте новый

# 2. User Token:
#    - Setup → Users → [Выберите пользователя]
#    - Вкладка "Remote access keys"
#    - "API token" → Создать новый токен
#    - Скопируйте сгенерированный токен

# 3. Настройка прав:
#    - Пользователю API нужны права на чтение принтеров
#    - Profile → [Профиль пользователя] → Assets → Printers: Read

# 4. Как узнать ID кастомного поля:
#    - В GLPI откройте карточку принтера
#    - Откройте DevTools браузера (F12) → Network
#    - Обновите страницу
#    - Найдите запрос к API с данными принтера
#    - В ответе найдите поле с серийным номером на бирке и его ID

# ========================================
# Безопасность
# ========================================
# ⚠️  НЕ КОММИТЬТЕ этот файл с реальными токенами в git!
# Храните токены только в .env (файл в .gitignore)
