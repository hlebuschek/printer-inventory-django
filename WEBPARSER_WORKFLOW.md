# 🔧 Руководство по использованию Конструктора действий WebParser

## Когда использовать конструктор действий?

Конструктор действий нужен, когда данные принтера находятся:
- На другой вкладке (нужно кликнуть на таб)
- В форме с аутентификацией (нужно ввести логин/пароль)
- На странице с динамической подгрузкой (нужно подождать)

## Пошаговый пример

### Сценарий: Счетчик находится на вкладке "Information"

#### Шаг 1: Загрузить страницу принтера
1. Выбрать протокол: HTTP
2. Путь: `/`
3. Нажать **"Загрузить страницу"**
4. Страница появится в предпросмотре

#### Шаг 2: Открыть конструктор
1. Нажать кнопку **"Открыть конструктор действий"**
2. Откроется модальное окно

#### Шаг 3: Построить цепочку действий

**Действие 1: Клик на вкладку**
- Нажать кнопку **"+ Клик"**
- CSS селектор: `#tab-information` (найти через DevTools)
- Ожидание после: `2` секунды

**Действие 2: Дождаться загрузки**
- Нажать кнопку **"+ Ожидание"**
- Секунд: `1`

**Действие 3: Парсить данные**
- Нажать кнопку **"+ Парсинг XPath"**
- XPath: `//td[contains(text(),'Total Pages')]/following-sibling::td/text()`
- Regex: `(\d+)` (извлечь только цифры)
- Имя переменной: `total_pages`

#### Шаг 4: Выполнить и проверить
1. Нажать **"Выполнить"**
2. В правой панели появится результат:
   ```
   ✓ Клик: #tab-information (ожидание 2с)
   ✓ Ожидание: 1с
   ✓ Парсинг: //td[contains...]/text() = "12345"
   ✓ Успешно!
   ```
3. Можно нажать **"Посмотреть HTML"** для проверки

#### Шаг 5: Сохранить действия
1. Нажать **"Сохранить действия"**
2. Модальное окно закроется
3. Под кнопкой появится превью:
   ```
   Действия:
   1. Клик: #tab-information (ожидание 2с)
   2. Ожидание: 1с
   3. Парсинг: //td[contains...]/text()
   ```

#### Шаг 6: Сохранить правило
1. Выбрать поле: **"Общий счетчик"**
2. Нажать **"Сохранить правило"**
3. Готово! ✅

---

## Типы действий

### 1. 🖱️ Клик (Click)
- **Что делает**: Кликает на элемент
- **Когда использовать**: Переход на вкладку, нажатие кнопки
- **Параметры**:
  - CSS селектор: `#tab2`, `.nav-link`, `button[name='submit']`
  - Ожидание после клика (сек): `1-5`

### 2. ⌨️ Ввод (Send Keys)
- **Что делает**: Вводит текст в поле
- **Когда использовать**: Форма с логином/паролем, поиск
- **Параметры**:
  - CSS селектор: `input[name='username']`
  - Текст для ввода: `admin`
  - Ожидание после (сек): `1-2`

### 3. ⏱️ Ожидание (Wait)
- **Что делает**: Ждет указанное время
- **Когда использовать**: Подгрузка данных после клика
- **Параметры**:
  - Секунд: `1-10`

### 4. 📋 Парсинг XPath (Parse)
- **Что делает**: Извлекает данные из HTML
- **Когда использовать**: ВСЕГДА в конце цепочки
- **Параметры**:
  - XPath: `//div[@id='counter']/text()`
  - Regex (опц.): `(\d+)` для извлечения цифр
  - Имя переменной: `counter_value`

---

## Примеры использования

### Пример 1: Простой парсинг (без кликов)
```
Действие 1: Парсинг
  XPath: //span[@class='total-pages']/text()
  Regex: (\d+)
```

### Пример 2: С кликом на вкладку
```
Действие 1: Клик
  Селектор: #status-tab
  Ожидание: 2 сек

Действие 2: Парсинг
  XPath: //div[@id='page-count']/text()
  Regex: (\d+)
```

### Пример 3: С формой входа
```
Действие 1: Клик
  Селектор: input[name='username']
  Ожидание: 0.5 сек

Действие 2: Ввод
  Селектор: input[name='username']
  Значение: admin
  Ожидание: 0.5 сек

Действие 3: Клик
  Селектор: input[name='password']
  Ожидание: 0.5 сек

Действие 4: Ввод
  Селектор: input[name='password']
  Значение: admin
  Ожидание: 0.5 сек

Действие 5: Клик
  Селектор: button[type='submit']
  Ожидание: 3 сек

Действие 6: Парсинг
  XPath: //td[@id='counter']/text()
  Regex: (\d+)
```

### Пример 4: С множественными вкладками
```
Действие 1: Клик (главная вкладка)
  Селектор: #main-tab
  Ожидание: 1 сек

Действие 2: Клик (подвкладка)
  Селектор: #info-subtab
  Ожидание: 2 сек

Действие 3: Ожидание (подгрузка данных)
  Секунд: 2

Действие 4: Парсинг
  XPath: //div[@class='counter-value']/text()
  Regex: (\d+)
```

---

## Как получить CSS селектор?

### Способ 1: Через DevTools (рекомендуется)
1. Нажать **"Загрузить страницу"** в WebParser
2. Открыть DevTools (F12)
3. Кликнуть на iframe в предпросмотре
4. Нажать на элемент (вкладка, кнопка)
5. В панели Elements найти элемент
6. Правой кнопкой → Copy → Copy selector
7. Вставить в поле "CSS селектор"

### Способ 2: Через новую вкладку
1. Нажать **"Открыть в новой вкладке"**
2. F12 → Elements
3. Выбрать элемент
4. Copy → Copy selector

### Примеры селекторов
- По ID: `#status-tab`, `#counter-value`
- По классу: `.nav-link`, `.btn-primary`
- По атрибуту: `button[name='submit']`, `input[type='text']`
- Сложные: `div.panel > table > tbody > tr:nth-child(2) > td`

---

## Как получить XPath?

### Через DevTools
1. Открыть страницу принтера
2. F12 → Elements
3. Найти элемент с нужными данными
4. Правой кнопкой → Copy → Copy XPath
5. Вставить в конструктор

### Примеры XPath
```
Простой текст:
//div[@id='counter']/text()

Текст в таблице:
//td[contains(text(),'Total Pages')]/following-sibling::td/text()

Атрибут:
//span[@class='toner-level']/@data-value

Сложный:
//table[@class='status']//tr[2]/td[3]/text()
```

---

## Устранение проблем

### Действия не выполняются
- ✅ Проверьте CSS селекторы (через DevTools)
- ✅ Увеличьте время ожидания
- ✅ Нажмите "Посмотреть HTML" для проверки

### Парсинг возвращает пустое значение
- ✅ Проверьте XPath через "Тестирование XPath" (левая панель)
- ✅ Убедитесь, что данные загрузились (добавьте ожидание)
- ✅ Проверьте, что вы на правильной вкладке

### Клик не работает
- ✅ Проверьте селектор
- ✅ Увеличьте время ожидания после клика
- ✅ Попробуйте другой селектор (ID вместо класса)

---

## Backend API

Конструктор действий использует следующий endpoint:

```
POST /inventory/api/web-parser/execute-action/

Body:
{
  "url": "http://192.168.1.100/",
  "actions": [
    {"type": "click", "selector": "#tab2", "wait": 2},
    {"type": "wait", "wait": 1},
    {"type": "parse", "xpath": "//div[@id='counter']/text()", "regex": "(\\d+)", "var_name": "counter"}
  ],
  "username": "admin",  // опционально
  "password": "admin"   // опционально
}

Response:
{
  "success": true,
  "html": "<html>...</html>",
  "action_log": [
    "✓ Клик: #tab2 (ожидание 2с)",
    "✓ Ожидание: 1с",
    "✓ Парсинг: //div[@id='counter']/text() = '12345'"
  ],
  "variables": {
    "counter": "12345"
  }
}
```

---

## Заключение

Конструктор действий позволяет:
1. ✅ Кликать на вкладки/кнопки
2. ✅ Вводить текст (логин/пароль)
3. ✅ Ждать подгрузки данных
4. ✅ Парсить данные из любого места

Все действия выполняются **последовательно** с помощью Selenium на backend.

**Важно**: Всегда заканчивайте цепочку действием "Парсинг" - оно извлекает данные!
