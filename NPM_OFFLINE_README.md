# 📦 Offline установка npm зависимостей

Быстрая инструкция для установки npm на сервере без интернета.

---

## ⚠️ ВАЖНО: Версия Node.js

**Сервер:** Node.js v18.20.4
**Ваш компьютер:** Должен быть Node.js v18.x.x

Если версии разные, используйте скрипт ниже или см. [подробную документацию](docs/NODE_VERSION_COMPATIBILITY.md).

---

## 🚀 Быстрый старт

### На компьютере с интернетом:

#### Вариант 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

```bash
# Скрипт автоматически переключится на Node 18 через nvm
./build-npm-bundle-for-server.sh
```

#### Вариант 2: Вручную

```bash
# 1. Установите nvm и переключитесь на Node 18
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.zshrc  # или ~/.bash_profile
nvm install 18
nvm use 18

# 2. Создайте архив
npm install
tar -czf npm-dependencies.tar.gz node_modules package.json package-lock.json
```

### На сервере без интернета:

```bash
# 1. Скопируйте файл на сервер
scp npm-dependencies.tar.gz user@server:/var/www/printer-inventory/

# 2. На сервере распакуйте
cd /var/www/printer-inventory
tar -xzf npm-dependencies.tar.gz

# 3. Проверьте
npm run build
```

---

## 📚 Документация

- **Быстрый старт:** [docs/QUICK_START_OFFLINE.md](docs/QUICK_START_OFFLINE.md)
- **Подробное руководство:** [docs/NPM_OFFLINE_INSTALL.md](docs/NPM_OFFLINE_INSTALL.md)
- **Совместимость версий:** [docs/NODE_VERSION_COMPATIBILITY.md](docs/NODE_VERSION_COMPATIBILITY.md)

---

## 🔍 Проверка версии Node.js

```bash
# На компьютере:
node -v  # Должно быть: v18.x.x

# На сервере:
ssh server "node -v"  # v18.20.4
```

Если версии разные → см. [docs/NODE_VERSION_COMPATIBILITY.md](docs/NODE_VERSION_COMPATIBILITY.md)

---

## 🐛 Устранение проблем

### "vite: not found"

```bash
# Проверьте, что node_modules распакован
ls -la node_modules/.bin/vite

# Если нет - распакуйте архив заново
```

### "Error: Cannot find module @esbuild/..."

```bash
# Проблема версии Node.js
# Пересоберите архив с Node.js 18 (см. выше)
```

### Подробнее

См. [docs/NODE_VERSION_COMPATIBILITY.md](docs/NODE_VERSION_COMPATIBILITY.md#диагностика-проблем)

---

## ✅ Готовые скрипты

1. `./build-npm-bundle-for-server.sh` - Сборка с правильной версией Node
2. `./create-npm-bundle.sh` - Простая быстрая сборка
3. `./npm-offline-setup.sh` - Полная подготовка с инструкциями

---

**Размер архива:** ~20-30 MB (сжатый), ~80-100 MB (распакованный)
