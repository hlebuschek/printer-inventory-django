# Руководство по миграции исторических данных из бэкапа

## Контекст

Бэкап от **1 октября 2025** содержит ценные исторические данные, которые были удалены автоматической очисткой (работает с 10 ноября 2025).

## Созданные инструменты

### 1. `analyze_backup` - Анализ бэкапа
Показывает что есть в бэкапе без изменения данных.

```bash
python manage.py analyze_backup --db-name backup
```

**Что показывает:**
- Общее количество записей
- Период данных (первый/последний опрос)
- Распределение по статусам
- Количество уникальных принтеров
- Сколько данных отсутствует в текущей БД
- Оценка размера после миграции

### 2. `migrate_from_backup` - Миграция данных
Переносит исторические данные с автоматическим сжатием.

```bash
# Сначала dry-run (анализ без изменений)
python manage.py migrate_from_backup --db-name backup --dry-run

# Реальная миграция всех отсутствующих данных
python manage.py migrate_from_backup --db-name backup

# Миграция только за конкретный год
python manage.py migrate_from_backup --db-name backup --year 2024
```

**Что делает:**
- ✅ Находит данные, которых нет в текущей БД
- ✅ Применяет сжатие: **1 последняя запись за день** для каждого принтера
- ✅ Переносит связанные PageCounter
- ✅ Избегает дубликатов
- ✅ Сопоставляет принтеры по IP/серийнику
- ✅ Показывает прогресс

### 3. `test_cleanup_logic` - Тест логики очистки
Показывает как будет работать очистка на текущих данных.

```bash
python manage.py test_cleanup_logic
```

## Пошаговая инструкция

### Шаг 1: Убедитесь что бэкап доступен

```bash
# Подключитесь к PostgreSQL
psql -U postgres -l

# Должна быть БД с именем 'backup'
```

Если бэкапа нет, восстановите его:

```bash
# Создайте БД
createdb -U postgres backup

# Восстановите из дампа
psql -U postgres backup < /path/to/backup_2025-10-01.sql
```

### Шаг 2: Проанализируйте бэкап

```bash
python manage.py analyze_backup --db-name backup
```

**Пример вывода:**
```
================================================================================
Анализ бэкапной базы данных: backup
================================================================================

📊 InventoryTask (история опросов):
  Всего записей: 1,245,678
  Первый опрос: 2024-01-15 08:00:00+00:00
  Последний опрос: 2025-09-30 23:45:12+00:00
  Период: 624 дней

📈 Распределение по статусам:
  SUCCESS: 1,180,234 (94.7%)
  FAILED: 45,123 (3.6%)
  HISTORICAL_INCONSISTENCY: 18,456 (1.5%)
  VALIDATION_ERROR: 1,865 (0.2%)

🖨️  Уникальных принтеров: 2,847

⏱️  Средняя частота: 1,996 опросов/день

📄 PageCounter (данные опросов):
  Всего записей: 1,180,234

📊 Статистика текущей БД (inventory_printer):
  Всего записей: 425,678
  Первый опрос: 2025-09-15 00:15:32+00:00
  Последний опрос: 2025-12-13 23:50:11+00:00

🔍 Анализ отсутствующих данных:
  Период отсутствующих данных: ~243 дней
  С 2024-01-15 по 2025-09-15
  Записей в бэкапе за этот период: 820,000
  После сжатия (1/день/принтер): ~691,641

💡 Рекомендации:
  1. Используйте команду 'migrate_from_backup' для переноса данных
  2. Данные будут автоматически сжаты (1 запись/день/принтер)
  3. Это сохранит полную историю при минимальном размере
```

### Шаг 3: Тестовый запуск миграции (dry-run)

```bash
python manage.py migrate_from_backup --db-name backup --dry-run
```

Это покажет **что будет перенесено** без реальных изменений.

### Шаг 4: Реальная миграция

```bash
# Миграция всех отсутствующих данных
python manage.py migrate_from_backup --db-name backup

# Или поэтапно по годам
python manage.py migrate_from_backup --db-name backup --year 2024
python manage.py migrate_from_backup --db-name backup --year 2025
```

**Внимание:** Миграция может занять время в зависимости от объёма данных.

### Шаг 5: Проверка результата

```bash
# Проверьте что данные появились
python manage.py shell
```

```python
from inventory.models import InventoryTask
from datetime import datetime

# Проверяем самую раннюю запись
oldest = InventoryTask.objects.order_by('task_timestamp').first()
print(f"Самая ранняя запись: {oldest.task_timestamp}")

# Проверяем количество записей
total = InventoryTask.objects.count()
print(f"Всего записей: {total:,}")

# Проверяем распределение по годам
from django.db.models.functions import ExtractYear
from django.db.models import Count

years = InventoryTask.objects.annotate(
    year=ExtractYear('task_timestamp')
).values('year').annotate(
    count=Count('id')
).order_by('year')

for item in years:
    print(f"  {item['year']}: {item['count']:,} записей")
```

## Технические детали

### Логика сжатия данных

Для каждой даты и принтера оставляется **только последняя запись**:

```sql
WITH daily_last AS (
    SELECT
        printer_id,
        DATE(task_timestamp) as date,
        MAX(id) as max_id
    FROM inventory_inventorytask
    GROUP BY printer_id, DATE(task_timestamp)
)
SELECT t.*
FROM inventory_inventorytask t
INNER JOIN daily_last dl ON t.id = dl.max_id
```

### Соответствие принтеров

Принтеры сопоставляются по IP-адресу или серийному номеру:
- Если принтер из бэкапа найден в текущей БД → данные переносятся
- Если принтер не найден → данные пропускаются (логируется warning)

### Избежание дубликатов

Проверяется комбинация `(printer_id, task_timestamp)`:
- Если такая запись уже есть → пропускается
- Если нет → импортируется

## Ожидаемые результаты

### Для системы с 3000 принтеров

**До миграции:**
- Данные с 14 сентября 2025 (90 дней назад)
- ~6.5 млн записей

**После миграции:**
- Данные с момента начала работы системы
- ~7.5 млн записей (прирост ~15%)
- Полная история за годы!

### Возможности после миграции

✅ **Графики печати** за весь период работы системы
✅ **Анализ динамики** по месяцам/годам
✅ **Сравнение периодов** (например, 2024 vs 2025)
✅ **Выявление трендов** использования принтеров

## Решение проблем

### "ModuleNotFoundError: No module named 'django'"

Активируйте виртуальное окружение:

```bash
source venv/bin/activate  # или где у вас venv
python manage.py analyze_backup --db-name backup
```

### "FATAL: database 'backup' does not exist"

Восстановите бэкап:

```bash
createdb -U postgres backup
psql -U postgres backup < /путь/к/бэкапу.sql
```

### "Принтер не найден в текущей БД"

Некоторые принтеры из бэкапа могли быть удалены. Это нормально, их данные просто пропускаются.

### Миграция работает очень долго

Используйте поэтапную миграцию по годам:

```bash
python manage.py migrate_from_backup --db-name backup --year 2024
# Подождите завершения
python manage.py migrate_from_backup --db-name backup --year 2025
```

## Безопасность

- ✅ Все операции выполняются в транзакции (откат при ошибке)
- ✅ Dry-run режим для предварительного анализа
- ✅ Исходный бэкап не изменяется
- ✅ Текущие данные не удаляются
- ✅ Только добавление отсутствующих записей

## Автор

Claude Code Assistant
Дата создания: 13 декабря 2025
